SECRET_KEY=your-secret-key-here
DEBUG=True

# MySQL (production)
DB_NAME=nasa_db
DB_USER=root
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=3306

# Set to True to use SQLite instead of MySQL (for local dev without MySQL)
USE_SQLITE=False
